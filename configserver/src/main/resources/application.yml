spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: /home/mt/demo/configserver/conf
server:
  port: 8888